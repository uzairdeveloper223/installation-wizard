#pragma once

/** The maximum length for locale string storage. */
#define MAX_LOCALE_LEN 64

/** The maximum length for disk path storage. */
#define MAX_DISK_LEN 64

/** The maximum number of partitions. */
#define MAX_PARTITIONS 16

/** The maximum length for mount point path. */
#define MAX_MOUNT_LEN 64

/** The maximum length for username. */
#define MAX_USERNAME_LEN 32

/** The maximum length for hostname. */
#define MAX_HOSTNAME_LEN 64

/** The maximum length for password. */
#define MAX_PASSWORD_LEN 128

/** The maximum number of users. */
#define MAX_USERS 8

/** The maximum number of options for locales/disks. */
#define MAX_OPTIONS 32
